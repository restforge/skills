# Generated — do not hand-edit

The `restforge-skills/` skill folder next to this file is a **generated mirror**.

**Single source of truth:** `restforge-skills/skills/restforge/`
(the standalone `create-restforge-skills` package at the workspace root).

To change the skill:

1. Edit the canonical skill under `restforge-skills/skills/restforge/`
   (`SKILL.md` + `references/`).
2. From the `restforge-skills/` folder, run `sync-to-plugin.bat` to mirror the
   content into `restforge-skills/` here.
3. Commit this plugin repo.

Hand-edits to `restforge-skills/` here are overwritten on the next sync.
